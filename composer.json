{
    "name": "escalated-dev/escalated-laravel",
    "description": "An embeddable support ticket system for Laravel applications",
    "keywords": ["laravel", "support", "tickets", "helpdesk", "customer-support"],
    "license": "MIT",
    "type": "library",
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/escalated-dev/escalated-locale"
        }
    ],
    "require": {
        "php": "^8.2",
        "escalated-dev/locale": "^0.1.8",
        "illuminate/contracts": "^11.0|^12.0|^13.0",
        "inertiajs/inertia-laravel": "^2.0|^3.0",
        "league/commonmark": "^2.0",
        "symfony/dom-crawler": "^6.0|^7.0"
    },
    "require-dev": {
        "laravel/pint": "^1.29",
        "orchestra/testbench": "^9.0|^10.0|^11.0",
        "pestphp/pest": "^2.0|^3.0",
        "pestphp/pest-plugin-laravel": "^2.0|^3.0|^4.0"
    },
    "autoload": {
        "psr-4": {
            "Escalated\\Laravel\\": "src/",
            "Escalated\\Laravel\\Database\\Seeders\\": "database/seeders/",
            "Escalated\\Laravel\\Database\\Factories\\": "database/factories/"
        },
        "files": [
            "src/Support/helpers.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Escalated\\Laravel\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Escalated\\Laravel\\EscalatedServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true
        },
        "audit": {
            "ignore": {
                "PKSA-5jz8-6tcw-pbk4": "phpunit advisory; dev-only test runner. pest 3.8.6 pins phpunit ^11.5.50 with conflict >11.5.50, leaving no advisory-clean version selectable until pest releases a new pin.",
                "PKSA-z3gr-8qht-p93v": "phpunit advisory; dev-only test runner. Same constraint as above.",
                "PKSA-mdq4-51ck-6kdq": "laravel/framework 11.x advisory; affects all released 11.x with no advisory-clean version, which blocks the L^11 compatibility matrix in Composer 2.9+. Ignored only in this root config (does NOT propagate to host apps); CI test-matrix only.",
                "PKSA-8qx3-n5y5-vvnd": "laravel/framework 11.x advisory; CI compat matrix only (see above).",
                "PKSA-q46n-4fdk-zjr4": "laravel/framework 11.x advisory; CI compat matrix only (see above).",
                "PKSA-qzrn-rnz3-85w1": "laravel/framework 11.x advisory; CI compat matrix only (see above).",
                "PKSA-w7xr-vk7n-rstm": "laravel/framework 11.x advisory; CI compat matrix only (see above)."
            }
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "archive": {
        "exclude": [
            "/docker",
            "/.dockerignore",
            "/docs",
            "/tests",
            "/.github"
        ]
    }
}
